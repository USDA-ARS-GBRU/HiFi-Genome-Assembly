# v0.4 Review Pass

Use this document as a working audit before tagging v0.4 or shifting primary development to v0.5 scaffolding.

## Current Strengths

- The curation workflow has an ordered index.
- Manual reference-to-assembly IGV review is documented.
- PAF and MUMmer dotplot options are represented.
- RagTag, Breakwright-style methods, and Pteranodon are framed as candidate generators.
- Accepted and rejected corrections are both modeled.
- FASTA splitting, breakpoint validation, correction summaries, decision audits, and report generation have toy tests.
- CI validates the major helper scripts and templates.

## Remaining v0.4 Review Questions

- Are all Step 8 README links current and nonredundant?
- Does the toy case study need a tiny illustrative dotplot image, or is command/text evidence enough for now?
- Should `make_correction_report.py` include parsed pass/fail summaries from validation TSVs?
- Should `post_correction_validation.sbatch` fail on FASTA header reviews, or continue to write review output with `|| true`?
- Are the IGV instructions clear enough for a new graduate student who has never created a custom genome in IGV?

## Release Candidate Gate

Before tagging v0.4, run:

```bash
scripts/check_repo_inventory.py --readme README.md -o /tmp/repo_inventory.tsv
python3 -m py_compile scripts/*.py
bash -n 01_sbatch_templates/*.sbatch
```

Then follow `docs/v0.4_release_candidate_checklist.md`.

## Decision

```text
ready_to_tag_v0.4: no
reason: Continue one more review pass after adding v0.5 scaffolding stubs and inventory checks.
next_focus: v0.5 scaffolding kickoff
```
